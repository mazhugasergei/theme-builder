{
  "name": "the-theme",
  "version": "0.0.4",
  "displayName": "The Theme",
  "publisher": "mazhugasergei",
  "engines": {
    "vscode": "*"
  },
  "contributes": {
    "themes": [
      {
        "label": "The Theme",
        "uiTheme": "vs-dark",
        "path": "./themes/theme.json"
      }
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mazhugasergei/theme-builder.git"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rimraf theme.vsix && rimraf themes",
    "build": "ts-node src/index.ts && vsce package --out theme.vsix",
    "install:code": "bash bin/install.sh code",
    "install:cursor": "bash bin/install.sh cursor",
    "install:windsurf": "bash bin/install.sh windsurf",
    "install:all": "npm run install:code && npm run install:cursor && npm run install:windsurf",
    "uninstall:code": "bash bin/uninstall.sh code",
    "uninstall:cursor": "bash bin/uninstall.sh cursor",
    "uninstall:windsurf": "bash bin/uninstall.sh windsurf",
    "uninstall:all": "npm run uninstall:code && npm run uninstall:cursor && npm run uninstall:windsurf",
    "reinstall:code": "npm run clean && npm run build && npm run uninstall:code && npm run install:code",
    "reinstall:cursor": "npm run clean && npm run build && npm run uninstall:cursor && npm run install:cursor",
    "reinstall:windsurf": "npm run clean && npm run build && npm run uninstall:windsurf && npm run install:windsurf",
    "reinstall:all": "npm run uninstall:all && npm run clean && npm run build && npm run install:all",
    "publish": "npm run clean && npm run build && vsce publish"
  },
  "dependencies": {
    "rimraf": "6.0.1",
    "shx": "0.4.0",
    "ts-node": "10.9.2",
    "vsce": "^2.15.0"
  }
}
